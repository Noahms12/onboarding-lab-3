#include <VExercise2.h>
#include <cstdint>

uint8_t step(uint8_t input){
  bool tap10 = (model.out % );
  bool tap8 = (model.out >> 8) & 1;
  bool tap3 = (model.out >> 3) & 1;
  bool tap1 = !(model.out % 2);
}

int main() {
  VExercise2 model;
  

}
